# Known Boundaries

- `v1.0.0` supports Codex CLI on Unix-like shells as the first-class target.
- native PowerShell-first Windows install flow is not a first-class target.
- complex existing global `AGENTS.md` stacks stop for manual review instead of auto-merge.
- validation is broader than maintainer-only proof, but not equivalent to broad public-scale community validation.
