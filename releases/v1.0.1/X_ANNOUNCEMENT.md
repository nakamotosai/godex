`godex` v1.0.1 is out.

This patch fixes the global dry-run safety regression from `v1.0.0`:

- preview no longer creates a missing Codex-home path
- preflight now reports missing Codex-home targets honestly
- install proof covers the regression directly

Lean scope, real fix, honest release.
