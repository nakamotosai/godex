`godex` public beta is up.

It upgrades vanilla Codex with stronger first-turn mirroring, verification-before-done, cleaner closeouts, and better review readability.

This beta now ships:

- project install
- global install
- doctor + restore for both
- clean-surface install proof

Still beta, not `v1`, but the install surface is now real.
