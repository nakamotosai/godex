# installers

This directory will hold installation and repair entrypoints for `godex`.

Planned flows:

- project-level install
- global install
- backup before mutation
- doctor handoff after install

Current stage:

- flow defined in spec
- scripts not implemented yet
